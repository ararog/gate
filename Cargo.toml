[package]
name = "rt-gate"
version = "0.1.4"
edition = "2021"
authors = ["Rogerio Araújo <rogerio.araujo@gmail.com>"]
repository = "https://github.com/ararog/gate"
homepage = "https://github.com/ararog/gate"
description = "A tiny wrapper for async runtimes."
readme = "README.md"
license = "MIT"
keywords = ["tasks", "async", "runtime", "wrapper"]
publish = true
rust-version = "1.64.0"

[package.metadata.docs.rs]
all-features = true

[features]
default = ["tokio-rt"]

smol-rt = ["dep:smol"]
tokio-rt = ["dep:tokio", "tokio/rt-multi-thread"]
compio-rt = ["compio/runtime"]

[dependencies]
compio = { version = "0.18.0", optional = true, default-features = false }
smol = { version = "2.0.2", optional = true, default-features = false }
tokio = { version = "1.50.0", default-features = false, optional = true }
